/**
 * Colores oficiales Karün / Wanda (fuente de verdad en TypeScript).
 * Las variables CSS del panel (`--panel-*`, `--brand-*`) están en `app/globals.css` :root;
 * si cambiás un hex acá, actualizá también `:root` para que coincidan.
 */

/** Logo 01 — bronce (acento UI, `--brand-accent-v1`). */
export const ACCENT_LOGO_V1 = "#a56a42";
/** Logo 02 — terracota profundo (`--brand-accent-v2`). */
export const ACCENT_LOGO_V2 = "#843921";

/** Rojo marca: hero, header, PWA theme (`--background`, `--brand-primary-red`). */
export const BRAND_PRIMARY_RED = "#963417";

/** Naranja UI (`--primary`, `--brand-ui-primary`). */
export const BRAND_UI_PRIMARY = "#d4602c";

/** Texto crema sobre fondos oscuros del panel. */
export const BRAND_CREAM = "#f5ebe3";
